import { RegisterForm } from "../components/RegisterForm.tsx";

const Register = () => {
  return (
    <main className={"w-full flex justify-center"}>
      <RegisterForm />
    </main>
  );
};

export { Register };

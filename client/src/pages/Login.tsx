import { LoginForm } from "../components/LoginForm.tsx";

const Login = () => {
  return (
    <main className={"w-full flex justify-center"}>
      <LoginForm />
    </main>
  );
};

export { Login };
